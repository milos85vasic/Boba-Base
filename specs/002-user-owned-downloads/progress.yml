# Execution state for feature 002-user-owned-downloads
# Written by /speckit-superspec-execute. Updated at each phase boundary.

feature: 002-user-owned-downloads
branch: 002-user-owned-downloads
updated: 2026-08-21

superpowers_detected:
  executing-plans: true
  subagent-driven-development: true
  test-driven-development: true

phases:
  - id: 1
    name: Setup (Shared Infrastructure)
    status: complete
    tasks: [T001, T002, T003]
    checkpoint:
      bash_n_clean: true
      yaml_parses: true          # 3 entries, schema_version 1
      functional_verified: true  # scope resolved, env expansion confirmed
      verdicts_exercised: [ok, absent, unwritable]
      verdicts_NOT_exercised: [wrong-owner]
  - id: 2
    name: Foundational (RED)
    status: complete
    tasks: [T004, T005, T006, T007]
    checkpoint:
      red_observed: true
      red_reason_verified: true   # assertions, not errors, not teardown
      integration_red: "uid 100999 vs operator 1000, both file and directory"
      unit_reds: "both name the missing target script explicitly"
      cleanup_verified: true      # 0 leftovers after run (podman unshare)
      killpg_audit: "no unguarded signals; 1 carrier-match comment, 1 guarded kill -0"
    deviations:
      - moved tests/integration/test_container_writes_owned_files.py to
        tests/ownership/ - tests/integration/conftest.py installs an AUTOUSE
        fixture that waits for the live merge service, so every test there needs
        the full stack UP. This test needs only a container RUNTIME. In the
        original location it hung 60s and ERRORED whenever the stack was down.
      - registered pytest marker `requires_runtime` in pyproject.toml
        (--strict-markers is on; `integration` was never registered)
    constraints_imposed_on_later_tasks:
      - T033 MUST treat an explicitly-empty CONTAINER_RUNTIME as "no container
        runtime" rather than re-detecting podman. The T006 suite sets it that way
        on every invocation, because podman IS installed and pre-build invariant
        30 executes every tests/unit/*.sh - an unguarded suite would launch real
        containers at gate time (§12.6/§12.12).
      - T033 must honour the --scope flag; the T006 suite deliberately does not
        also set OWNED_PATHS_FILE, so an implementation that ignores --scope is
        caught.

  - id: 3
    name: US1 - downloads usable
    status: complete-pending-review
    # CORRECTED 2026-08-22: this file read "not-started" for phases 3-6 while
    # tasks.md showed 38 of 42 tasks checked. tasks.md was right; THIS file was
    # stale. Verified on disk before correcting rather than trusting either
    # document: start.sh carries 6 references to the ownership precondition /
    # repair split, both scripts/ownership_*.sh exist, and the evidence
    # directory holds baseline-start.log, T013-baseline.md, T014-recreate.log,
    # T014-T017-us1-green.md and T018-review-NOGO.md.
    # A stale execution-state file is the §11.4.186 cross-document divergence
    # class, and it is the more dangerous half: a reader trusting it would
    # conclude the work was never begun and redo it.
    status_note: |
      T018 CLOSED 2026-08-25 — GO, zero findings, zero warnings, round 4
      (evidence/T018-review-GO.md). Four review rounds on the §11.4.209
      substrate, iterated per §11.4.134. Substantive answer: on the current
      docker-compose.yml no service mounting an in-scope path defeats
      operator-owned writes — userns_mode active keys 0, user: keys 0,
      PUID=0/PGID=0 preserved on the two linuxserver services (deliberate),
      store 600, world-writable under config/ 0, group-writable 0.
      Round 4 also corrected the reviewer's own round-3 audit: a bracket-only
      instrument saw 41 of 50 gate labels and reported one denominator while
      two were live.
      Does NOT discharge T042 or the §11.4.185 manual-QA gate.
  - id: 4
    name: US2 - backlog repaired
    status: complete-pending-review
    open_task: T028   # [REVIEW] carries a recorded ORDERING VIOLATION, not laundered
  - id: 5
    name: US3 - session lifecycle
    status: complete
  - id: 6
    name: Polish
    status: not-started

open_findings:
  - id: X1
    severity: HIGH
    raised_at: phase-1-checkpoint
    title: probe_location asserts host-write ownership, not container-write ownership
    detail: >
      probe_location() reports "ok" for the download root, a directory owned by
      uid 100999 that still contains a 100999-owned item. It runs as the host
      user, so it creates a host-owned file and concludes the location is fine.
      The defect this feature exists to fix is that CONTAINER writes land at
      100999 - so the probe answers a different question than FR-010 asks.
      This is the exact proxy-vs-real-condition failure (§11.4.201) that
      FR-010b and the function's own docstring forbid.
    scope: T003 built what its task specified; the defect is in
      contracts/startup-precondition.md, which did not state WHOSE write must
      be probed.
    status: RESOLVED 2026-08-21 - operator approved option C (both probes,
      each reported for what it proves). contracts/startup-precondition.md
      amended; T033 must implement P1 + P2 with the honest-SKIP fallback.
    blocks: nothing - resolution recorded before Phase 2 proceeds
    options_for_operator:
      - Probe by running a throwaway container write (真 condition; needs the
        runtime up at precondition time, and costs seconds per start)
      - Probe host-write AND assert the per-service route from
        docker-compose.yml (cheap, but the route check is itself a proxy)
      - Both - container probe when the runtime is available, route assertion
        as the fallback, each reported honestly for what it proves

# RELOCATED 2026-08-22: this block sat at 2-space indent INSIDE the `phases:`
# list, where YAML expects a list item — so this file has never parsed. Moved
# to top level unchanged in content. A machine-readable execution-state file
# that no machine can read is a §11.4.201(6) false-null at the tooling layer:
# any consumer either errors or silently skips it, and either way the state it
# records is never actually consulted.
known_gaps_from_phase_2:
  - "E4 wrong-owner verdict is unreachable in unit tests: creating a file owned
    by another uid needs privilege. Only the container probe (P1) produces the
    uid-100999 shape. Unit suites cover unwritable/absent instead - decisive per
    the contract, but not the shape the feature targets."
  - "tests/integration/conftest.py hangs 60-180s and errors instead of skipping
    honestly (§11.4.3) when the stack is down. Pre-existing, affects other tests,
    NOT fixed here - out of scope and shared infrastructure."
