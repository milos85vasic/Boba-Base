# Opt-in quality stack for qBittorrent-Fixed.
#
# This file is intentionally separate from docker-compose.yml so that
# the runtime Container-First principle (.specify/memory/constitution.md
# Principle I) is preserved: the two-container product topology does
# not mandate this stack, and no product service depends on it. Quality
# tooling is additive and only started on demand.
#
#   Usage (rootless Podman or Docker):
#     $COMPOSE_CMD -f docker-compose.yml \
#                  -f docker-compose.quality.yml up -d sonarqube
#
#     # Or run scanners one-shot (no long-lived containers):
#     ./scripts/scan.sh --all
#
# See docs/QUALITY_STACK.md and docs/SCANNING.md for justification,
# environment variables, and how this stack interacts with CI.

version: "3.8"

services:
  sonarqube:
    image: sonarqube:10-community
    container_name: qbit-sonarqube
    profiles: [quality, sonarqube]
    depends_on:
      sonar-db:
        condition: service_healthy
    environment:
      SONAR_JDBC_URL: jdbc:postgresql://sonar-db:5432/sonar
      SONAR_JDBC_USERNAME: ${SONAR_DB_USER:-sonar}
      SONAR_JDBC_PASSWORD: ${SONAR_DB_PASSWORD:-sonar}
      # Disable the built-in H2 (not for production) — we use postgres.
      SONAR_ES_BOOTSTRAP_CHECKS_DISABLE: "true"
    ports:
      - "127.0.0.1:9000:9000"
    volumes:
      - sonarqube_data:/opt/sonarqube/data
      - sonarqube_logs:/opt/sonarqube/logs
      - sonarqube_extensions:/opt/sonarqube/extensions
    ulimits:
      nofile:
        soft: 65536
        hard: 65536
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:9000/api/system/status | grep -q '\"status\":\"UP\"'"]
      interval: 30s
      timeout: 10s
      retries: 30
      start_period: 120s
    restart: unless-stopped
    # Container hygiene (CONST-033 corollary, boba-jackett pattern): dies before
    # the user session under host pressure. Sized generously — bundled JVM +
    # embedded Elasticsearch + compute engine is the heaviest service in this
    # stack (SonarQube's own docs recommend >=2 GiB, often 4 GiB in practice).
    mem_limit: 4g
    pids_limit: 1024
    oom_score_adj: 500

  sonar-db:
    image: postgres:16-alpine
    container_name: qbit-sonar-db
    profiles: [quality, sonarqube]
    environment:
      POSTGRES_USER: ${SONAR_DB_USER:-sonar}
      POSTGRES_PASSWORD: ${SONAR_DB_PASSWORD:-sonar}
      POSTGRES_DB: sonar
    volumes:
      - sonar_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${SONAR_DB_USER:-sonar}"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped
    # Container hygiene (CONST-033 corollary, boba-jackett pattern): a single
    # SonarQube-only postgres instance, moderate connection pool — well below
    # the JVM sonarqube service, above the lightweight one-shot scanner CLIs.
    mem_limit: 1g
    pids_limit: 256
    oom_score_adj: 500

  # One-shot scanners. Run via `./scripts/scan.sh <name>`.
  # The `run-once` profile keeps them out of `up -d` by default.
  snyk:
    image: snyk/snyk:python
    container_name: qbit-snyk
    profiles: [run-once, snyk]
    # Container hygiene (CONST-033 corollary, boba-jackett pattern): one-shot
    # scanner over a Python dependency tree — bounded, no long-lived process.
    mem_limit: 1g
    pids_limit: 256
    oom_score_adj: 500
    environment:
      SNYK_TOKEN: ${SNYK_TOKEN:-}
    working_dir: /project
    volumes:
      - ./:/project:ro
      - ./artifacts/scans:/artifacts
    command: >-
      sh -c "
      if [ -z \"$$SNYK_TOKEN\" ]; then
        echo 'SNYK_TOKEN unset; skipping snyk scan';
        exit 0;
      fi;
      snyk auth --auth-token=$$SNYK_TOKEN >/dev/null &&
      snyk test --file=download-proxy/requirements.txt --sarif-file-output=/artifacts/snyk.sarif || true;
      snyk code test --sarif-file-output=/artifacts/snyk-code.sarif || true
      "

  semgrep:
    image: returntocorp/semgrep:latest
    container_name: qbit-semgrep
    profiles: [run-once, semgrep]
    # Container hygiene (CONST-033 corollary, boba-jackett pattern): one-shot
    # rule-based static analysis over the whole tree — the heaviest of the
    # scanner CLIs (full-repo AST matching across many `--config=auto` rules).
    mem_limit: 2g
    pids_limit: 512
    oom_score_adj: 500
    working_dir: /src
    volumes:
      - ./:/src:ro
      - ./artifacts/scans:/artifacts
    command: >-
      semgrep scan --config=auto --config=.semgrep.yml
        --sarif -o /artifacts/semgrep.sarif

  trivy:
    image: aquasec/trivy:latest
    container_name: qbit-trivy
    profiles: [run-once, trivy]
    # Container hygiene (CONST-033 corollary, boba-jackett pattern): one-shot
    # filesystem vuln/secret/misconfig scan, bounded by the repo tree size.
    mem_limit: 1g
    pids_limit: 256
    oom_score_adj: 500
    working_dir: /src
    volumes:
      - ./:/src:ro
      - ./artifacts/scans:/artifacts
      - trivy_cache:/root/.cache/trivy
    command: >-
      fs --scanners vuln,secret,misconfig
         --severity HIGH,CRITICAL
         --format sarif
         --output /artifacts/trivy-fs.sarif
         --exit-code 0
         /src

  gitleaks:
    image: zricethezav/gitleaks:latest
    container_name: qbit-gitleaks
    profiles: [run-once, gitleaks]
    # Container hygiene (CONST-033 corollary, boba-jackett pattern): one-shot
    # regex-based secret scan — the lightest scanner CLI in this stack.
    mem_limit: 512m
    pids_limit: 128
    oom_score_adj: 500
    working_dir: /src
    volumes:
      - ./:/src:ro
      - ./artifacts/scans:/artifacts
    command: >-
      detect --source /src
             --config /src/.gitleaks.toml
             --report-format sarif
             --report-path /artifacts/gitleaks.sarif
             --exit-code 0

  # Observability stack (Phase 6). Profile = observability so it stays
  # off by default. Grafana dashboards live under observability/dashboards/.
  prometheus:
    image: prom/prometheus:latest
    container_name: qbit-prometheus
    profiles: [observability]
    ports:
      - "127.0.0.1:9090:9090"
    volumes:
      - ./observability/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - --config.file=/etc/prometheus/prometheus.yml
      - --storage.tsdb.retention.time=7d
      - --web.listen-address=:9090
    restart: unless-stopped
    # Container hygiene (CONST-033 corollary, boba-jackett pattern): long-running
    # TSDB with a 7d retention window — moderate, bounded working set.
    mem_limit: 1g
    pids_limit: 256
    oom_score_adj: 500

  grafana:
    image: grafana/grafana-oss:latest
    container_name: qbit-grafana
    profiles: [observability]
    depends_on: [prometheus]
    environment:
      GF_SECURITY_ADMIN_USER: ${GRAFANA_USER:-admin}
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_PASSWORD:-admin}
      GF_AUTH_ANONYMOUS_ENABLED: "false"
    ports:
      - "127.0.0.1:3000:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./observability/dashboards:/etc/grafana/provisioning/dashboards:ro
      - ./observability/datasources:/etc/grafana/provisioning/datasources:ro
    restart: unless-stopped
    # Container hygiene (CONST-033 corollary, boba-jackett pattern): long-running
    # dashboard/web UI — lighter than prometheus, no local TSDB of its own.
    mem_limit: 512m
    pids_limit: 256
    oom_score_adj: 500

volumes:
  sonarqube_data:
  sonarqube_logs:
  sonarqube_extensions:
  sonar_db_data:
  trivy_cache:
  prometheus_data:
  grafana_data:
