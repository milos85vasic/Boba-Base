# Fixture: the escape sentinel used to waive a TRAILING marker on a live
# statement. A documentation mention has no reason to hang off executable
# code, so the waiver is refused there (C5) — this is the residue shape.
def check(x):
    return True  # MUTATED for §11.4.115 RED  # guardrails:allow documenting the scan
