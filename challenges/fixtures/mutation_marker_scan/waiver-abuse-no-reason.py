# Fixture: the escape sentinel used as a BARE bypass — no reason given.
# §11.4.224(E) fence: an unjustified exclusion is refused, so this is a
# finding (C5), not a waiver.
def check(x):
    # MUTATED for §11.4.115 RED  # guardrails:allow
    return True
