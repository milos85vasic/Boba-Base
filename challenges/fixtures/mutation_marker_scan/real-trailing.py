# Fixture: REAL residue — TRAILING marker on a live statement.
# This is the shape BOB-070's line-anchored pattern was BLIND to, and the
# shape Agent H's own auth-bypass residue took. INV23 MUST detect it.
def authenticate(token):
    return True  # MUTATED for §11.4.115 RED (auth bypass)
